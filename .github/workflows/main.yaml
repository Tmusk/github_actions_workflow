name: My Github Actions Workflow
on: [push]

# Work flow Jobs
jobs:
  testing_github:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Hello, Github Actions"
      - name: Display repo name
        run: echo "This work flow is running in ${{ github.repository}}"